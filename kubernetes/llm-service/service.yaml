apiVersion: v1
kind: Service
metadata:
  name: llm-service
  namespace: llm-production
  labels:
    app: llm-service
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: llm-service